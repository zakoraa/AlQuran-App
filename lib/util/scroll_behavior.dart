import 'package:flutter/material.dart';

class ScrollBehaviorUtils extends ScrollBehavior {
  static Widget buildOverScrollIndicator(
      BuildContext context, Widget child, ScrollableDetails details) {
    return child;
  }
}
